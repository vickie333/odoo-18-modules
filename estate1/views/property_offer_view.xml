<odoo>
    <record id="estate_property_offer_view_form" model="ir.ui.view">
        <field name="name">estate.property.offer.form</field>
        <field name="model">estate.property.offer</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <group>
                        <group>
                            <field name="price" widget="monetary"/>
                             <field name="partner_id"/>
                        </group>
                        <group>
                            <field name="validity"/>
                            <field name="date_deadline" widget="date"/>
                            <field name="status"/>
                        </group>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="estate_property_offer_view_list" model="ir.ui.view">
        <field name="name">estate.property.offer.list</field>
        <field name="model">estate.property.offer</field>
        <field name="arch" type="xml">
            <list editable="bottom"
                  decoration-success="status == 'accepted'"
                  decoration-warning="status == 'refused'">
                <field name="price" widget="monetary" />
                <field name="partner_id" />
                <field name="validity" />
                <field name="date_deadline"/>
                <field name="property_type_id" />
                <button name="action_accept_offer" type="object" icon="fa-check" class="btn-plain icon-success" invisible="status == 'accepted' or status == 'refused'"/>
                <button name="action_refuse_offer" type="object" icon="fa-times" class="btn-plain icon-danger" invisible="status == 'accepted' or status == 'refused'"/>
            </list>
        </field>
    </record>

    <record id="estate_property_offer_act_window" model="ir.actions.act_window">
        <field name="name">estate.property.offer.act.window</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">estate.property.offer</field>
        <field name="domain">[('property_type_id', '=', active_id)]</field>
        <field name="view_mode">list</field>
    </record>
</odoo> 